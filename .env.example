# Copy to .env.local for local dev. For production (e.g. GitHub Actions / host), set these as build secrets.
VITE_SUPABASE_URL=https://YOUR_PROJECT_REF.supabase.co
VITE_SUPABASE_ANON_KEY=your_anon_key
